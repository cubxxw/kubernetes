interface:
  display_name: "Kubernetes"
  short_description: "Repo-specific patterns and workflows for kubernetes"
  default_prompt: "Use the kubernetes repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true